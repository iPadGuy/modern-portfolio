{
	"name": "modern_portfolio",
	"version": "1.0.0",
	"description": "URL:",
	"main": "index.js",
	"scripts": {
		"browse": "sleep 3s && firefox --private-window http://localhost:3000",
		"dev": "npm-run-all --parallel sass serve browse",
		"sass": "node-sass -w scss -o dist/css/ --recursive",
		"serve": "live-server --no-browser --port=3000 dist/",
		"serve1": "algernon -a -n -t -x dist/"
	},
	"keywords": [],
	"author": "Patrick Allan",
	"license": "MIT",
	"dependencies": {
		"gh-pages": "^5.0.0",
		"node-sass": "^9.0.0"
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5"
	}
}
