{
	"name": "modern_portfolio",
	"version": "1.0.0",
	"description": "URL:",
	"main": "index.js",
	"scripts": {
		"browse": "firefox --private-window http://localhost:3000",
		"dev": "npm-run-all --parallel sass serve browse",
		"npm-check": "npm-check -u",
		"sass": "node-sass -w scss -o dist/css/ --recursive",
		"serve": "algernon -a -n -x dist/"
	},
	"keywords": [],
	"author": "Patrick Allan",
	"license": "MIT",
	"dependencies": {
		"gh-pages": "^5.0.0",
		"node-sass": "^9.0.0"
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5"
	}
}
